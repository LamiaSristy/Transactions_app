<h1>Hello</h1>

<div class='center jumbotron shadow'>
  <article>
  <h3>All Transactions With Group:</h3>
  Total Expence<%= @t1.sum(:amount) %>
  <ul class="list-unstyled">
    <% @t1.each do |t| %>
    <li>
      <%= t.name %>
       <%= t.amount %>
    </li>
  <% end %>
  </ul>  
</article>
</div>

<div class='action'>
  <%= link_to 'Add new transaction', new_transaction_path, :class => 'btn btn-primary btn-lg' %> 
</div>